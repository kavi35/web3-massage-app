{
  "name": "blockchain-messenger",
  "version": "1.0.0",
  "description": "Simple messaging application on Base L2 network",
  "main": "script.js",
  "scripts": {
    "start": "npx http-server -p 8000",
    "deploy:base": "npx hardhat run scripts/deploy.js --network base",
    "deploy:base-sepolia": "npx hardhat run scripts/deploy.js --network baseSepolia"
  },
  "keywords": [
    "blockchain",
    "ethereum",
    "base",
    "messaging",
    "web3",
    "decentralized"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "hardhat": "^2.17.0"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "ethers": "^6.0.0"
  }
}
